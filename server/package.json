{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend server for BackgroundAI application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo 'No build step needed for server'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write **/*.{js,json}",
    "format:check": "prettier --check **/*.{js,json}",
    "clean": "rm -rf uploads/ temp/",
    "serve": "npm run start"
  },
  "keywords": ["express", "nodejs", "openai", "background", "image"],
  "author": "Your Name",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.6.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.0"
  }
}